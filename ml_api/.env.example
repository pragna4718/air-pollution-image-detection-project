# Example environment variables for ml_api
# Copy this file to .env and fill in your keys (do NOT commit .env to source control)

# Gemini / Generative AI API key (optional). If provided, the server will call Gemini to refine Vision outputs.
GEMINI_API_KEY=

# (Optional) alternate key name that server will also check
GOOGLE_API_KEY=

